[package]
name = "mqtt-wx"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "=4.6.1", features = ["derive"] }
env_logger = "=0.11.10"
log = "=0.4.29"

[build-dependencies]

[dev-dependencies]

[profile.release]
strip = true
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
